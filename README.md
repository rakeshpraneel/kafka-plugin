# kafka-plugin
kafka-plugin is a open source python package which ease out the kafka integration part. It runs the consumer instance in seperate thread and stores the messages in a local queue which can be then used in your application.  This can be easily integrated to your code base just with a yaml manifest which holds the required kafka configurations.
